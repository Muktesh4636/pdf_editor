# Copy to .env.deploy and `set -a; source .env.deploy; set +a` before ./deployment.sh
# Or export inline:  DEPLOY_HOST=... ./deployment.sh

DEPLOY_HOST=root@72.61.148.117
DEPLOY_REMOTE=/var/www/pdfforge.store/

# Optional: non-default SSH key
# RSYNC_RSH='ssh -i ~/.ssh/id_ed25519 -o StrictHostKeyChecking=accept-new'
